# User database — manage on the host, never commit real credentials
data/users.txt

# Refresh log
data/refresh.log

# SSH keys — never commit private keys
*.pem
id_rsa
id_ed25519
*.key

# OS/editor junk
.DS_Store
*.swp
*~
